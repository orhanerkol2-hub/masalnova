---
import BaseLayout from '../../layouts/BaseLayout.astro';

const game = {
  title: 'Keloğlan: Uçan Tohumlar',
  shortDescription: 'Keloğlan’la tohum toplarını sapanla saksılara ulaştır, doğru açıyı bul ve üç Anadolu bahçesini çiçeklendir.',
  image: '/games/keloglan-ucan-tohumlar/cover.jpg',
  imageAlt: 'Keloğlan Uçan Tohumlar oyununda Keloğlan, sapan, uçan tohum ve açan çiçekler',
};

const gameUrl = 'https://masalnova.com/oyna/keloglan-ucan-tohumlar/';

const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'VideoGame',
    name: game.title,
    description: game.shortDescription,
    url: gameUrl,
    image: `https://masalnova.com${game.image}`,
    inLanguage: 'tr-TR',
    applicationCategory: 'Game',
    genre: ['Physics puzzle', "Children's game"],
    gamePlatform: ['Web browser', 'Mobile web'],
    playMode: 'SinglePlayer',
    typicalAgeRange: '5-8',
    publisher: { '@type': 'Organization', name: 'MasalNova' },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Ana Sayfa', item: 'https://masalnova.com/' },
      { '@type': 'ListItem', position: 2, name: 'Oyunlar', item: 'https://masalnova.com/oyunlar/' },
      { '@type': 'ListItem', position: 3, name: game.title, item: gameUrl },
    ],
  },
];
---
<BaseLayout
  title={`${game.title} Oyna | MasalNova`}
  description={`${game.shortDescription} MasalNova’ya özel, reklamsız ve ücretsiz Türkçe tarayıcı oyununu oyna.`}
  path="/oyna/keloglan-ucan-tohumlar/"
  image={game.image}
  imageAlt={game.imageAlt}
  activeNav="oyunlar"
  allowAds={false}
  jsonLd={jsonLd}
>
  <section class="game-page">
    <div class="container">
      <nav class="breadcrumbs" aria-label="İçerik yolu">
        <a href="/">Ana Sayfa</a><span aria-hidden="true">›</span>
        <a href="/oyunlar/">Oyunlar</a><span aria-hidden="true">›</span>
        <span>Keloğlan: Uçan Tohumlar</span>
      </nav>

      <div class="game-heading">
        <div>
          <div class="meta-row">
            <span class="exclusive-pill"><i class="ph ph-seal-check" aria-hidden="true"></i> MasalNova’ya özel</span>
            <span>Fizik ve Beceri</span>
            <span>Öneri: 5+</span>
          </div>
          <h1>Keloğlan: Uçan Tohumlar</h1>
          <p>{game.shortDescription}</p>
        </div>
        <div class="heading-actions">
          <a class="fullscreen-link" href="/games/keloglan-ucan-tohumlar/index.html" data-fullscreen-trigger>
            <i class="ph ph-arrows-out" aria-hidden="true"></i> Tam ekran oyna
          </a>
          <a class="back-link" href="/oyunlar/"><i class="ph ph-grid-four" aria-hidden="true"></i> Tüm oyunlar</a>
        </div>
      </div>

      <section class="player-shell" aria-label="Keloğlan Uçan Tohumlar oyun alanı" data-player-shell>
        <iframe
          src="/games/keloglan-ucan-tohumlar/index.html"
          title="Keloğlan: Uçan Tohumlar — MasalNova oyun alanı"
          allow="fullscreen"
          allowfullscreen
          loading="eager"
          data-game-frame
        ></iframe>
        <a
          class="mobile-game-launch"
          href="/games/keloglan-ucan-tohumlar/index.html"
          aria-label="Tam ekran oyna ve macerayı başlat"
          data-fullscreen-trigger
          data-auto-start
        >
          <span class="mobile-game-launch-label">
            <i class="ph ph-arrows-out" aria-hidden="true"></i>
            <span><strong>Tam ekran oyna</strong><small>Dokun ve macerayı başlat</small></span>
          </span>
        </a>
      </section>

      <p class="local-note">
        <i class="ph ph-shield-check" aria-hidden="true"></i>
        Bu oyun MasalNova tarafından aynı alan adı üzerinden sunulur ve oyun içi reklam içermez. En iyi puanın yalnızca bu cihazda saklanır.
      </p>

      <div class="game-info-grid">
        <article class="info-card">
          <span class="info-icon"><i class="ph ph-hand-swipe-left" aria-hidden="true"></i></span>
          <div>
            <h2>Nasıl oynanır?</h2>
            <p>Keloğlan’ın gösterdiği tohum topunu geriye çek, beyaz noktalı uçuş yoluna bak ve saksıya doğru bırak. Telefonda parmağını, bilgisayarda fareyi kullanabilirsin.</p>
          </div>
        </article>
        <article class="info-card">
          <span class="info-icon"><i class="ph ph-flower-lotus" aria-hidden="true"></i></span>
          <div>
            <h2>Hedefin</h2>
            <p>Keloğlan’ın Köyü, Bilge Çeşme ve Yayla Terası’ndaki bütün saksıları çiçeklendir. Farklı açıları dene; kullanılmayan her tohum bonus puan kazandırır.</p>
          </div>
        </article>
      </div>

      <aside class="learning-note">
        <i class="ph ph-lightbulb-filament" aria-hidden="true"></i>
        <div>
          <strong>Oynarken keşfet</strong>
          <p>Açı, güç ve uçuş yolu arasındaki ilişkiyi denemek; neden-sonuç kurma, tahmin ve el-göz koordinasyonunu destekler. Süre sınırı ve kaybetme cezası yoktur.</p>
        </div>
      </aside>
    </div>
  </section>
</BaseLayout>

<style>
  .game-page { padding: 28px 0 58px; }
  .game-page .container { max-width: 1260px; }
  .breadcrumbs { display: flex; flex-wrap: wrap; align-items: center; gap: 8px; color: rgba(255,255,255,.72); font-size: .78rem; font-weight: 800; }
  .breadcrumbs a:hover { color: #fff; text-decoration: underline; }
  .game-heading { display: flex; align-items: end; justify-content: space-between; gap: 28px; margin: 22px 0 18px; color: #fff; }
  .meta-row { display: flex; flex-wrap: wrap; gap: 7px; margin-bottom: 10px; }
  .meta-row span { display: inline-flex; align-items: center; gap: 5px; padding: 5px 10px; border: 1px solid rgba(255,255,255,.28); border-radius: 999px; background: rgba(255,255,255,.12); font-size: .73rem; font-weight: 900; }
  .meta-row .exclusive-pill { border-color: #ffe18b; background: #ffd25b; color: #5f3700; }
  h1 { font-size: clamp(2rem, 5vw, 3.65rem); letter-spacing: -.04em; }
  .game-heading p { max-width: 760px; margin-top: 9px; color: rgba(255,255,255,.82); }
  .heading-actions { display: flex; flex: 0 0 auto; gap: 9px; }
  .fullscreen-link, .back-link { display: inline-flex; align-items: center; gap: 8px; padding: 10px 14px; border: 1px solid rgba(255,255,255,.3); border-radius: 999px; background: rgba(255,255,255,.12); font-size: .8rem; font-weight: 900; }
  .fullscreen-link { border-color: #ffd45b; background: #ffd45b; color: #593600; }
  .fullscreen-link:hover, .back-link:hover { transform: translateY(-2px); }
  .player-shell { position: relative; overflow: hidden; border: 1px solid rgba(255,255,255,.24); border-radius: 26px; background: #153967; box-shadow: 0 28px 68px rgba(0,15,66,.38); }
  .player-shell iframe { display: block; width: 100%; height: min(720px, calc(100dvh - 132px)); min-height: 560px; border: 0; background: #153967; }
  .mobile-game-launch { display: none; }
  .player-shell:fullscreen { width: 100vw; height: 100dvh; border: 0; border-radius: 0; background: #153967; box-shadow: none; }
  .player-shell:fullscreen iframe { width: 100%; height: 100%; min-height: 100%; pointer-events: auto; }
  .player-shell:fullscreen .mobile-game-launch { display: none; }
  :global(html.game-fullscreen-active), :global(body.game-fullscreen-active) { height: 100%; overflow: hidden !important; overscroll-behavior: none; }
  .local-note { display: flex; align-items: flex-start; gap: 9px; margin: 14px 4px 0; color: rgba(255,255,255,.74); font-size: .78rem; }
  .local-note i { flex: 0 0 auto; margin-top: 2px; color: #ffd957; font-size: 1rem; }
  .game-info-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 18px; margin-top: 18px; }
  .info-card { display: flex; gap: 15px; padding: 22px; border: 1px solid rgba(255,255,255,.18); border-radius: 20px; background: rgba(255,255,255,.11); color: #fff; }
  .info-icon { display: grid; flex: 0 0 auto; width: 42px; height: 42px; place-items: center; border-radius: 13px; background: #ffd14b; color: #173169; font-size: 1.3rem; }
  .info-card h2 { font-size: 1.08rem; }
  .info-card p { margin-top: 6px; color: rgba(255,255,255,.75); font-size: .88rem; }
  .learning-note { display: flex; align-items: flex-start; gap: 13px; margin-top: 18px; padding: 18px 21px; border: 1px solid rgba(255,225,119,.3); border-radius: 18px; background: rgba(255,213,77,.13); color: #fff; }
  .learning-note > i { flex: 0 0 auto; margin-top: 2px; color: #ffd957; font-size: 1.45rem; }
  .learning-note strong { font-size: .92rem; }
  .learning-note p { margin-top: 3px; color: rgba(255,255,255,.73); font-size: .82rem; }

  @media (min-width: 721px) {
    .game-page { background: #fbf8f1; color: #102a66; }
    .breadcrumbs { color: #71809a; }
    .breadcrumbs a:hover { color: #1749ae; }
    .game-heading { color: #0b2b75; }
    .game-heading p { color: #68758d; }
    .meta-row span { border-color: #d9dfec; background: #fff; color: #3c5686; }
    .meta-row .exclusive-pill { border-color: #e8bd3f; background: #ffe189; color: #694000; }
    .back-link { border-color: #d6deee; background: #fff; color: #1749ae; }
    .player-shell { border-color: #d6d0c3; box-shadow: 0 24px 56px rgba(26,38,76,.16); }
    .local-note { color: #68758d; }
    .local-note i { color: #148a52; }
    .info-card { border-color: #ded9ce; background: #fff; color: #173574; box-shadow: 0 12px 30px rgba(23,43,92,.07); }
    .info-card p { color: #69748a; }
    .learning-note { border-color: #e8d493; background: #fff8dd; color: #173574; }
    .learning-note > i { color: #a97807; }
    .learning-note p { color: #69748a; }
  }

  @media (max-width: 820px) {
    .game-heading { align-items: flex-start; flex-direction: column; }
    .heading-actions { width: 100%; }
    .fullscreen-link { justify-content: center; flex: 1; min-height: 44px; }
    .back-link { display: none; }
    .player-shell { touch-action: pan-y; }
    .player-shell iframe { height: min(660px, calc(100dvh - 184px)); min-height: 500px; pointer-events: none; }
    .mobile-game-launch { position: absolute; inset: 0; z-index: 4; display: flex; align-items: flex-end; justify-content: center; padding: 22px; background: transparent; touch-action: pan-y; }
    .mobile-game-launch-label { display: inline-flex; align-items: center; gap: 10px; min-width: 210px; padding: 12px 18px; border: 1px solid rgba(255,255,255,.72); border-radius: 999px; background: #ffd45b; color: #593600; box-shadow: 0 12px 30px rgba(0,15,66,.34); pointer-events: none; }
    .mobile-game-launch-label > i { flex: 0 0 auto; font-size: 1.25rem; }
    .mobile-game-launch-label > span { display: grid; text-align: left; line-height: 1.05; }
    .mobile-game-launch strong { font-size: .84rem; }
    .mobile-game-launch small { margin-top: 4px; font-size: .67rem; font-weight: 800; opacity: .72; }
    .player-shell:fullscreen { touch-action: none; }
    .player-shell:fullscreen iframe { height: 100%; min-height: 100%; pointer-events: auto; }
    .game-info-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 480px) {
    .game-page { padding-top: 22px; }
    .game-heading { gap: 18px; margin-top: 18px; }
    .player-shell { margin-inline: -12px; border-radius: 20px; }
    .player-shell iframe { height: min(680px, calc(100dvh - 174px)); min-height: 500px; }
  }
</style>

<script>
  const playerShell = document.querySelector<HTMLElement>('[data-player-shell]');
  const gameFrame = document.querySelector<HTMLIFrameElement>('[data-game-frame]');
  const fullscreenTriggers = document.querySelectorAll<HTMLAnchorElement>('[data-fullscreen-trigger]');

  if (playerShell && gameFrame && fullscreenTriggers.length > 0) {
    let wasFullscreen = false;

    const startEmbeddedGame = (attempt = 0) => {
      try {
        const startButton = gameFrame.contentDocument?.querySelector<HTMLButtonElement>('[data-action-value="start"]');
        if (startButton) {
          startButton.click();
          gameFrame.focus({ preventScroll: true });
          return;
        }
      } catch {
        // The standalone fallback below covers browsers that isolate the frame.
      }

      if (attempt < 10) {
        window.setTimeout(() => startEmbeddedGame(attempt + 1), 100);
      }
    };

    const setFullscreenState = () => {
      const isFullscreen = document.fullscreenElement === playerShell;
      document.documentElement.classList.toggle('game-fullscreen-active', isFullscreen);
      document.body.classList.toggle('game-fullscreen-active', isFullscreen);

      if (isFullscreen) {
        gameFrame.focus({ preventScroll: true });
      } else if (wasFullscreen) {
        gameFrame.src = gameFrame.src;
      }

      wasFullscreen = isFullscreen;
    };

    const openGame = async (event: MouseEvent) => {
      const trigger = event.currentTarget as HTMLAnchorElement;
      const shouldAutoStart = trigger.hasAttribute('data-auto-start');
      event.preventDefault();

      if (document.fullscreenElement === playerShell) {
        if (shouldAutoStart) startEmbeddedGame();
        gameFrame.focus({ preventScroll: true });
        return;
      }

      try {
        if (playerShell.requestFullscreen) {
          await playerShell.requestFullscreen();
          setFullscreenState();
          if (shouldAutoStart) startEmbeddedGame();
          return;
        }
      } catch {
        // iOS Safari and restricted embeds use the standalone game as fallback.
      }

      const fallbackUrl = new URL(trigger.href, window.location.href);
      if (shouldAutoStart) fallbackUrl.searchParams.set('autostart', '1');
      window.location.assign(fallbackUrl.href);
    };

    fullscreenTriggers.forEach((trigger) => trigger.addEventListener('click', openGame));
    document.addEventListener('fullscreenchange', setFullscreenState);
    window.addEventListener('pagehide', () => {
      document.documentElement.classList.remove('game-fullscreen-active');
      document.body.classList.remove('game-fullscreen-active');
    });
  }
</script>
